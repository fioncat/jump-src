# jump-src

Install:

```shell
git clone https://github.com/fioncat/jump-src.git
cd jump-src
go install
```

Create domain:

```shell
jump-src init --domain github.com --ssh
```

Usage:

```shell
uhome <TAB>
usearch  # Need fzf
```
